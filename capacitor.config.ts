import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aryan.iaryan',
  appName: 'Insta',
  webDir: 'build',
  server: {
    cleartext: true,
  },
};

export default config;
